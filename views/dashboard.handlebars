{{#each postMetaData as |post|}}
<div class="editAllPosts">
    <div>
        <h4>{{post.title}}</h4>
        <p>{{post.text}}</p>
        <p>{{post.createdAt}}</p>
        <p>Posted by: {{post.user.user_name}}</p>
    </div>
</div>
{{/each}}

{{!-- need to create another landing page --}}